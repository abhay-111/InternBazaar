const config = {
  apikey: "abcd",
  mongoapikey:
    "mongodb+srv://<username>:<password>@cluster0.vnmzf.mongodb.net/<dbname>?retryWrites=true&w=majority",
  mailgunapikey: "xyz",
  mailgundomain: "abc.mailgun.org",
  tokenkey: "your-token-key",
};

module.exports = config;
